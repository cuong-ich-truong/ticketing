module.exports = {
  webpack: (config) => {
    config.watchOptions.poll = 300;
    return config;
  },
  experimental: {
    turbo: {
      rules: {
        '*.svg': {
          loaders: ['@svgr/webpack'],
          as: '*.js',
        },
      },
    },
  },
};
